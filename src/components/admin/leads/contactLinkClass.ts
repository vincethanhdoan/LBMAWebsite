// Shared visual treatment for phone-tool links (Call, Text a reminder) so the
// detail panel and list rows render one consistent style.
export const CONTACT_LINK_CLASS =
  'inline-flex items-center justify-center gap-1.5 min-h-12 px-3 rounded-md border border-border text-[13px] font-medium hover:bg-muted transition-colors';
